import { ExtendedRegExp } from '../extended-regexp/extended_regexp.js';
import { toList } from '../utils/to_list.js';
class PatternCrafter {
    constructor(spec, data) {
        this.spec = spec;
        this.data = data;
    }
    craft() {
        return toList(this.data.settings.template)
            .map(template => {
            var _a, _b;
            return new ExtendedRegExp(new RegExp(this.spec.compose(template), (_a = this.data.settings.flags) !== null && _a !== void 0 ? _a : ''), template, (_b = this.data.settings.map) !== null && _b !== void 0 ? _b : false);
        });
    }
}
export { PatternCrafter };
